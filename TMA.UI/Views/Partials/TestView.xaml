<UserControl x:Class="TMA.UI.Views.Partials.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Width="400" Height="200"
                VerticalAlignment="Center" Margin="20"
                Background="#FF76767F">
        <StackPanel.Resources>
            <Style x:Key="mybtn" TargetType="{x:Type Button}">
                <Setter Property="Width" Value="120" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Foreground" Value="#FFAAC3DA" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="FontStretch" Value="UltraCondensed" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="brdr"
                                    CornerRadius="20"
                                    BorderThickness="2"
                                    BorderBrush="#FF091B80"
                                    Background="#FF4E63A2">

                                <ContentPresenter x:Name="cPresenter"
                                                  Content="{TemplateBinding Content}"                                                  
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="mytxt" TargetType="{x:Type TextBox}">
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Foreground" Value="#FF373665" />
                <Setter Property="Height" Value="35" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border x:Name="brdr"
                                    CornerRadius="20"
                                    BorderThickness="2"
                                    BorderBrush="#FF5E5D8E"
                                    Background="#FFBFBFCC">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Padding="10,6"
                                              SnapsToDevicePixels="True" 
                                              VerticalScrollBarVisibility="Hidden"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalAlignment="Center"
                                              Focusable="False" >
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </StackPanel.Resources>
        <Button Style="{StaticResource mybtn}" Content="Button" />

        <TextBox Width="120" Margin="20"
                 Style="{StaticResource mytxt}"
                 Text="df" />
    </StackPanel>
</UserControl>
