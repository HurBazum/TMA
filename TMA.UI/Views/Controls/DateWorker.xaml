<UserControl x:Class="TMA.UI.Views.Controls.DateWorker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TMA.UI.Views.Controls"
             xmlns:sys="clr-namespace:System;assembly=netstandard"
             xmlns:conv="clr-namespace:TMA.UI.Infrastructure.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="AliceBlue">
    <UserControl.Resources>
        <ResourceDictionary>
            <sys:String x:Key="Day">_Day</sys:String>
            <sys:String x:Key="Increment">_Increment</sys:String>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="TextAlignment" Value="Center" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontWeight" Value="DemiBold" />
            </Style>
            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource clndrBtn}">
                <Setter Property="Command" Value="{Binding Cmd, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <Setter Property="Background" Value="#FF7EC1DE" />
                <Setter Property="IsEnabled" Value="{Binding Limit, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            </Style>
            <Style TargetType="{x:Type StackPanel}">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Grid.Row" Value="1" />
            </Style>
        </Grid.Resources>
        <StackPanel Grid.Row="0" Grid.ColumnSpan="3" Orientation="Horizontal">

            <TextBlock Text="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <CheckBox x:Name="HasLimit"
                      Margin="20 0"
                      IsChecked="{Binding Limit, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      Style="{StaticResource ccb}"/>
        </StackPanel>

        <StackPanel Grid.Column="0">
            <Button Content="▲" CommandParameter="{Binding Parameters[0], RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <TextBlock Text="{Binding Day, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Button Content="▼" CommandParameter="{Binding Parameters[3], RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </StackPanel>
        <StackPanel Grid.Column="1">
            <Button Content="▲" CommandParameter="{Binding Parameters[1], RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <TextBlock Text="{Binding Month, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource monthsConverter}}" />
            <Button Content="▼" CommandParameter="{Binding Parameters[4], RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </StackPanel>
        <StackPanel Grid.Column="2">
            <Button Content="▲" CommandParameter="{Binding Parameters[2], RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <TextBlock Text="{Binding Year, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Button Content="▼" CommandParameter="{Binding Parameters[5], RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </StackPanel>
    </Grid>
</UserControl>
