<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="baseBox" TargetType="{x:Type TextBox}">
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="#FF373665" />
        <Setter Property="FontFamily" Value="Century Gothic" />
    </Style>
    
    <Style x:Key="mytxt" TargetType="{x:Type TextBox}" BasedOn="{StaticResource baseBox}">
        <Setter Property="Height" Value="35" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="brdr"
                        CornerRadius="20"
                        BorderThickness="2"
                        BorderBrush="#FF5E5D8E"
                        Background="#FFBFBFCC">
                        <ScrollViewer x:Name="PART_ContentHost"
                                  Padding="10,6"
                                  SnapsToDevicePixels="True" 
                                  VerticalScrollBarVisibility="Hidden"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalAlignment="Center"
                                  Focusable="False" >
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="bigBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource baseBox}">
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="AcceptsReturn" Value="True" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="DemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="brdr"
                            BorderThickness="2"
                            BorderBrush="#FF5E5D8E"
                            Background="#FFBFBFCC">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Padding="10,6"
                                      SnapsToDevicePixels="True"
                                      VerticalScrollBarVisibility="Hidden"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalAlignment="Top"
                                      Focusable="False">                            
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>